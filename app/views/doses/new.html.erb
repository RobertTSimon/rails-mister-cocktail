<div class="container">
  <h1>Add Ingredients</h1>
  <%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.association :ingredient %>
  <%= f.input :description %>
  <%= f.submit 'Add ingredients and dose!', class: "btn btn-primary" %>
  <% end %>
</div>
